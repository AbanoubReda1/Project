@model IEnumerable<StudentService.Models.Task>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TaskPath)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type.TypeDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TaskPath)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Type.TypeDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TaskNo }) |
            @Html.ActionLink("Details", "Details", new { id=item.TaskNo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TaskNo })
        </td>
    </tr>
}

</table>
