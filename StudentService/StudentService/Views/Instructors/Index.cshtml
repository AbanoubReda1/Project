@model IEnumerable<StudentService.Models.Instructor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.InstructorName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.InstructorName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InstructorID.Trim() }) |
            @Html.ActionLink("Details", "Details", new { id=item.InstructorID.Trim() }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.InstructorID.Trim() })
        </td>
    </tr>
}

</table>
