@model IEnumerable<StudentService.Models.Course>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AutoID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CrediteNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Syllabus)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Department.DepartmentName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AutoID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CrediteNo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Syllabus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Department.DepartmentName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CourseNo }) |
            @Html.ActionLink("Details", "Details", new { id=item.CourseNo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CourseNo })
        </td>
    </tr>
}

</table>
